export interface Clients {
	id: number;
	name: string;
	address: string;
	phone: string;
}

export interface Products {
	id: number;
	name: string;
	price: number;
	stock: number;
}

export interface Workers {
	id: number;
	name: string;
}

export interface Sells {
	id: number;
	name: string;
}